<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match spring.logs>
  @type elasticsearch
  host localhost
  port 9200
  scheme https
  user elastic
  password YbBdWCNYrZ23k5PNxt39
  ssl_verify false          				# set to true if you have proper certificates
  logstash_format true       				# automatically adds @timestamp
  include_tag_key true       				# include the Fluentd tag in each document
  tag_key tag                				# the field name for the tag
  index_name spring-logs     				# base index name
  type_name _doc             				# ES 8.x+ ignores this, but required for Fluentd plugin
  time_key @timestamp        				# field name for event timestamp
  time_key_format %Y-%m-%dT%H:%M:%S.%N%:z
  flush_interval 5s          				# flush buffer every 5 seconds
</match>